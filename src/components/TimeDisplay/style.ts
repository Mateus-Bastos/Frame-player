import styled from 'styled-components';

export const TimeDisplay = styled.div`
  color: white;
  font-size: 14px;  // Aumenta o tamanho da fonte
  font-family: 'Work Sans', sans-serif;  // Aplicando a fonte Work Sans
`;